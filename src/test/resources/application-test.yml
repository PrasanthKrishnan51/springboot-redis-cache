spring:
  data:
    mongodb:
      # Uses embedded Flapdoodle MongoDB — no real Mongo needed in tests
      auto-index-creation: true
    redis:
      host: localhost
      port: 6379
  cache:
    type: simple   # Use in-memory cache for tests (no Redis required)

logging:
  level:
    com.example.app: DEBUG
